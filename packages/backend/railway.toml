[build]
builder = "nixpacks"
buildCommand = "cd packages/backend && cargo build --release"

[deploy]
startCommand = "cd packages/backend && cargo run --release"
healthcheckPath = "/health"
restartPolicy = "on-failure"